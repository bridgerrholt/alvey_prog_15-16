// Converts any variable to a string (doesn't fail if passed a string).

/*#ifndef CONVERT_STRING_H
#define CONVERT_STRING_H

#include <string>
#include <patch/to_string.h>

//std::string convertString(const std::string& input);
//std::string convertString(const char* input);
template<class T>
std::string convertString(const T& input);



template<class T>
std::string convertString(const T& input)
{
	return std::to_string(input);
}


#endif*/